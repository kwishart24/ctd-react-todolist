import styled from 'styled-components';

export const StyledTodosViewForm = styled.form`
  padding: 1em;

  div {
    margin: 1em 0;
  }

  label {
    margin: 0 0.5em;
  }

  select,
  input {
    padding: 0.5em;
    text-align: left;
  }
`;
